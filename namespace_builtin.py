print(dir(__builtins__))

#built-in namespace is the highest level of namespace.  Whenever we run a python application, we are provided a built-in namespace that is created when the inerpreter
# is started and has a lifetime until the interpreter terminates.